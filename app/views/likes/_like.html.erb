<% if product.already_liked?(current_user) %>
  <%= link_to 'いいねを取り消す', product_like_path(product, current_user.likes.find_by(product_id: product.id)), method: :delete %> 
<% else %>
  <%= link_to 'いいねする', product_likes_path(product.id), method: :post %>
<% end %>
